name: Android Release Build

on:
  workflow_dispatch:

jobs:
  build-android:
    # Build a release Android APK only when explicitly requested.
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0  # Fetch all history for version generation

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # Cache Gradle dependencies and wrapper for faster builds
      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Decode signing key
        run: echo $SIGNING_KEY | base64 -d > src/ui/flutter_app/android/app/key.jks
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          cache: true  # Enable Flutter SDK caching

      # Cache Pub dependencies for faster dependency resolution
      - name: Cache Pub packages
        uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Export environment variables
        run: export

      - name: Create App version
        run: |
          git fetch --tags
          git tag
          git log -n1
          bash -x ./version.sh

      - name: Print Flutter SDK version
        run: flutter --version

      - name: Install dependencies
        run: bash -x ./flutter-init.sh

      # Build release Android APK only (no lint check, no AAB, no external uploads)
      - name: Build release Android APK
        run: |
          cd src/ui/flutter_app
          cp android/app/build.gradle_github android/app/build.gradle
          flutter build apk -v --dart-define=SANMILL_ONLINE_BASE_URL="$SANMILL_ONLINE_BASE_URL"
        env:
          KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          ALIAS: ${{ secrets.ALIAS }}
          KEY_PATH: key.jks
          SANMILL_ONLINE_BASE_URL: ${{ vars.SANMILL_ONLINE_BASE_URL }}
          SANMILL_ONLINE_HOST: ${{ vars.SANMILL_ONLINE_HOST }}

      # Upload release APK as GitHub Actions artifact for download
      - name: Upload release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: sanmill-android-apk-release-master
          path: src/ui/flutter_app/build/app/outputs/flutter-apk/app-release.apk
