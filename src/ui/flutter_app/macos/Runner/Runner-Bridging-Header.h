// SPDX-License-Identifier: AGPL-3.0-or-later
// Copyright (C) 2019-2026 The Sanmill developers (see AUTHORS file)

// Runner-Bridging-Header.h
//
// Used to import the legacy `MillEngine` ObjC class from
// `command/mill_engine_ios.h` -- that source tree was removed in
// Phase 3 / Phase 4 along with the C++ engine, so this header is
// intentionally empty now.  Kept (rather than deleted) because the
// Xcode project still lists it as the bridging header.
