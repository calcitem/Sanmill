/// Enumeration of available place animation effects.
enum PlaceEffectType {
  ripple,
  expansion,
}

/// Enumeration of available remove animation effects.
enum RemoveEffectType {
  particles,
  fade,
}
